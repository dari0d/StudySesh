<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project3">
        <activity android:name=".StatsSummaryActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".EditItemsActivity" />
        <activity android:name=".NewItemsActivity" />
        <activity android:name=".EditSeshActivity" />
        <activity android:name=".EditClassActivity" />
        <activity android:name=".NewSeshActivity" />
        <activity android:name=".NewClassActivity" />
        <activity android:name=".LiveSessionActivity" />
        <activity android:name=".LogStatsActivity" />


        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>